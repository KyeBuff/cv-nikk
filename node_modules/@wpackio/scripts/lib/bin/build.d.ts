import { ProgramOptions } from './index';
/**
 * Start the `wpackio-scripts build` command.
 *
 * @param options Option as received from CLI.
 */
export declare function build(options: ProgramOptions | undefined): void;

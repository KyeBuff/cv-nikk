export declare type issueType = {
    origin: 'typescript' | 'internal';
    code: string;
    severity: 'error' | 'warning';
    message: string;
    file: string;
    location: {
        start: {
            line: number;
            column: number;
        };
        end: {
            line: number;
            column: number;
        };
    };
};
export declare function typescriptFormatter(issue: issueType, cwd: string): string;

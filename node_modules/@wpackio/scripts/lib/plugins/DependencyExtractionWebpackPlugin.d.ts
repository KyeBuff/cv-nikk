import webpack, { Compiler } from 'webpack';
declare type DependencyExtractionWebpackPluginOptions = {
    appDir: string;
    gutenbergOptimized: boolean;
};
export declare class DependencyExtractionWebpackPlugin {
    private externalizedDeps;
    private externalsPlugin;
    private compiler;
    private assetDependenciesData;
    private options;
    constructor(options?: DependencyExtractionWebpackPluginOptions);
    externalizeWpDeps(_context: any, request: string, callback: any): any;
    mapRequestToDependency(request: string): string;
    stringify(asset: any): string;
    /**
     * Write the asset manifest to the file system.
     *
     * @param {string} destination
     */
    writeTo(destination: string, content: string): Promise<void>;
    handleEmit(compilation: webpack.compilation.Compilation): void;
    handleAfterEmit(): Promise<void>;
    apply(compiler: Compiler): void;
}
export {};
